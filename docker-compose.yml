services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend/cache:/app/cache
    restart: unless-stopped

  # frontend: (will be added in next phase)
